<%= form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.number_field :number, :value => 2, :min => 1 %> TICKETS AT $8
  </div>

  <div class="actions" id="customButton", data-key="<%= Rails.configuration.stripe[:publishable_key] %>">
     <%= button_tag "B U Y", class: 'button' %>
  </div>

<% end %>
